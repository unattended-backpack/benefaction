GetTest
├── when reading beyond the data length
│   └── it reverts with {InvalidOffset}
└── when reading within the data length
    ├── when not reading a multiple of 8 bytes
    │   └── it reverts with {InvalidOffset}
    └── when reading a multiple of 8 bytes
        └── it returns mps and block delta